# Backups

Versioned notebook and text copies are written here by default. Generated
contents are intentionally ignored by Git.

Use `configureRICHPaths[]` in an interactive Wolfram notebook to select a
different per-user backup directory.
